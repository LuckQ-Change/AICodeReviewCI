---
review: ai
---
# 命名与可读性（AI 语义审查）

仅在 diff 中出现明确违反时报告，并必须提供 evidence 引用。

- 函数命名：使用动词或动词短语，表达明确的动作与意图。
- 变量命名：语义清晰，避免 `data1`/`temp1` 等非语义化命名；布尔值使用 `is/has/should` 前缀。
- 文件与目录：名称表达模块职责；避免过度缩写。
- 对外 API/事件：命名稳定、可读，避免频繁变更。
